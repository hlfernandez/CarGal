# Agnostic Events Manager Platform (AEMP)

## Docker deployment

Using `Docker Compose version v2.18.1`:

```shell
docker compose build && docker compose up
```

Run `docker compose rm` for removing the exited containers.
